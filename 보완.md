# AutoEmbed LAB 소스 번들 리뷰 및 오류 수정

## 📋 오류 요약

| # | 심각도 | 파일 | 오류 |
|---|--------|------|------|
| 1 | 🔴 빌드 차단 | `I18nProvider.tsx` | `../public/locales/...` 경로 해결 실패 |
| 2 | 🔴 빌드 차단 | `package.json` | `react-i18next`, `i18next` 의존성 누락 |
| 3 | 🔴 빌드 차단 | `App.tsx` | `ErrorBoundary` 미정의/미임포트 |
| 4 | 🔴 빌드 차단 | `App.tsx` | `useSavedProjects`, `projectById` 미임포트 |
| 5 | 🔴 빌드 차단 | `LanguageSwitcher.tsx` | `cn` 유틸 미임포트 |
| 6 | 🟠 테스트 실패 | `GitHubDeploy.test.tsx` | `btn.innerText` undefined (jsdom) |
| 7 | 🟠 테스트 실패 | `interpreter.test.ts` | struct/union 파싱 버그 |
| 8 | 🟡 오타 | `storage.ts` | `AUTOEMBER_` → `AUTOEMBED_` |
| 9 | 🟡 테스트 인프라 | `package.json` | `@testing-library/*` 미설치 |

---

## 🔴 수정 1: `I18nProvider.tsx` — 경로 오류 (빌드 차단의 근본 원인)

**문제**: `public/` 디렉토리는 Vite에서 정적 서빙 전용이며, ES import로 가져올 수 없습니다.

**해결**: 번역 파일을 `src/locales/`로 이동하고 import 경로를 수정합니다.

### 파일 이동
```
public/locales/ko/translation.json  →  src/locales/ko/translation.json
public/locales/de/translation.json  →  src/locales/de/translation.json
public/locales/en/translation.json  →  src/locales/en/translation.json
```

### `src/providers/I18nProvider.tsx` (수정)
```tsx
import { I18nProvider as ReactI18nextProvider, useTranslation } from 'react-i18next';
import i18n from 'i18next';
import { initReactI18next } from 'react-i18next';

// ✅ 수정: src/locales/ 에서 import
import koTranslation from '../locales/ko/translation.json';
import deTranslation from '../locales/de/translation.json';
import enTranslation from '../locales/en/translation.json';

i18n
  .use(initReactI18next)
  .init({
    resources: {
      ko: { translation: koTranslation },
      de: { translation: deTranslation },
      en: { translation: enTranslation },
    },
    lng: 'ko',
    fallbackLng: 'ko',
    interpolation: {
      escapeValue: false,
    },
  });

export const I18nProvider = ({ children }: { children: React.ReactNode }) => {
  return <ReactI18nextProvider i18n={i18n}>{children}</ReactI18nextProvider>;
};

export const useI18n = () => {
  const { t, i18n } = useTranslation();
  return { t, i18n };
};
```

---

## 🔴 수정 2: `package.json` — 누락된 의존성 추가

```json
{
  "dependencies": {
    "clsx": "2.1.1",
    "i18next": "^23.11.0",
    "jszip": "^3.10.1",
    "react": "19.2.6",
    "react-dom": "19.2.6",
    "react-i18next": "^14.1.0",
    "tailwind-merge": "3.4.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "4.1.17",
    "@testing-library/jest-dom": "^6.4.0",
    "@testing-library/react": "^15.0.0",
    "@testing-library/user-event": "^14.5.0",
    "@types/node": "22.19.17",
    "@types/react": "19.2.7",
    "@types/react-dom": "19.2.3",
    "@vitejs/plugin-react": "5.1.1",
    "jsdom": "^24.0.0",
    "tailwindcss": "4.1.17",
    "typescript": "5.9.3",
    "vite": "7.3.2",
    "vite-plugin-singlefile": "2.3.0",
    "vitest": "^4.1.10"
  }
}
```

---

## 🔴 수정 3: `App.tsx` — 누락된 import + ErrorBoundary 정의

```tsx
import React, { useCallback, useState, Suspense, lazy, Component } from 'react';
import type { TabId } from './types';
import Navbar from './components/Navbar';
import Footer from './components/Footer';
import { LanguageSwitcher } from './components/LanguageSwitcher';
// ✅ 추가: 누락된 import
import { useSavedProjects } from './hooks/useSavedProjects';
import { projectById } from './data/projects';

// Lazy-loaded tab components
const Dashboard = lazy(() => import('./components/Dashboard'));
const Universities = lazy(() => import('./components/Universities'));
const Roadmap = lazy(() => import('./components/Roadmap'));
const Portfolio = lazy(() => import('./components/Portfolio'));
const Competitiveness = lazy(() => import('./components/Competitiveness'));
const LabExercises = lazy(() => import('./components/LabExercises'));
const GuideTab = lazy(() => import('./components/GuideTab'));
const Hero = lazy(() => import('./components/Hero'));
const Generator = lazy(() => import('./components/Generator'));

// ✅ 추가: Error Boundary 컴포넌트 정의
class ErrorBoundary extends Component<
  { children: React.ReactNode },
  { hasError: boolean; error: Error | null }
> {
  constructor(props: { children: React.ReactNode }) {
    super(props);
    this.state = { hasError: false, error: null };
  }
  static getDerivedStateFromError(error: Error) {
    return { hasError: true, error };
  }
  componentDidCatch(error: Error, info: React.ErrorInfo) {
    console.error('ErrorBoundary caught:', error, info);
  }
  render() {
    if (this.state.hasError) {
      return (
        <div className="grid min-h-[400px] place-items-center p-8 text-center">
          <div>
            <p className="text-4xl">⚠️</p>
            <h2 className="mt-4 text-xl font-bold text-white">렌더링 오류가 발생했습니다</h2>
            <p className="mt-2 text-sm text-slate-400">
              {this.state.error?.message || '알 수 없는 오류'}
            </p>
            <button
              onClick={() => this.setState({ hasError: false, error: null })}
              className="mt-4 rounded-xl bg-amber-400 px-5 py-2.5 text-sm font-bold text-black"
            >
              다시 시도
            </button>
          </div>
        </div>
      );
    }
    return this.props.children;
  }
}

export default function App() {
  const [tab, setTab] = useState<TabId>('home');
  const [focusProjectId, setFocusProjectId] = useState<string | null>(null);
  const { savedIds, toggle, remove, clear } = useSavedProjects();

  const changeTab = useCallback((t: TabId) => {
    setTab(t);
    window.scrollTo({ top: 0, behavior: 'auto' });
    const targetH2 = document.querySelector('main h2');
    if (targetH2) {
      (targetH2 as HTMLElement).focus();
    }
  }, []);

  const handleViewProject = useCallback((id: string) => {
    setFocusProjectId(id);
    changeTab('generator');
  }, [changeTab]);

  const scrollToGenerator = () => {
    document.getElementById('generator')?.scrollIntoView({ behavior: 'smooth', block: 'start' });
  };

  const savedProjects = savedIds
    .map((id) => projectById(id))
    .filter((p): p is NonNullable<typeof p> => Boolean(p));

  return (
    <div className="min-h-screen bg-[#07090d] text-slate-200">
      <Navbar active={tab} onChange={changeTab} savedCount={savedIds.length} />
      <div className="absolute top-0 right-0 p-2 z-50">
        <LanguageSwitcher />
      </div>
      <main>
        <ErrorBoundary>
          <Suspense fallback={<div className="p-8 text-center text-slate-400">로딩 중...</div>}>
            {tab === 'home' && (
              <Dashboard savedProjects={savedProjects} onNavigate={changeTab} onView={handleViewProject} />
            )}
          </Suspense>
          <Suspense fallback={<div className="p-8 text-center text-slate-400">로딩 중...</div>}>
            {tab === 'generator' && (
              <>
                <Hero onGenerate={scrollToGenerator} onUni={() => changeTab('universities')} />
                <Generator
                  focusProjectId={focusProjectId}
                  onConsumeFocus={() => setFocusProjectId(null)}
                  savedIds={savedIds}
                  onToggleSave={toggle}
                />
              </>
            )}
          </Suspense>
          <Suspense fallback={<div className="p-8 text-center text-slate-400">로딩 중...</div>}>
            {tab === 'universities' && <Universities savedProjects={savedProjects} />}
          </Suspense>
          <Suspense fallback={<div className="p-8 text-center text-slate-400">로딩 중...</div>}>
            {tab === 'roadmap' && <Roadmap />}
          </Suspense>
          <Suspense fallback={<div className="p-8 text-center text-slate-400">로딩 중...</div>}>
            {tab === 'portfolio' && (
              <Portfolio
                savedProjects={savedProjects}
                onView={handleViewProject}
                onRemove={remove}
                onClear={clear}
                onGoGenerate={() => changeTab('generator')}
              />
            )}
          </Suspense>
          <Suspense fallback={<div className="p-8 text-center text-slate-400">로딩 중...</div>}>
            {tab === 'compete' && <Competitiveness savedProjects={savedProjects} />}
          </Suspense>
          <Suspense fallback={<div className="p-8 text-center text-slate-400">로딩 중...</div>}>
            {tab === 'labs' && <LabExercises savedProjects={savedProjects} />}
          </Suspense>
          <Suspense fallback={<div className="p-8 text-center text-slate-400">로딩 중...</div>}>
            {tab === 'guide' && <GuideTab savedProjects={savedProjects} />}
          </Suspense>
        </ErrorBoundary>
      </main>
      <Footer />
    </div>
  );
}
```

---

## 🔴 수정 4: `LanguageSwitcher.tsx` — `cn` import 누락

```tsx
import { useState, useEffect } from 'react';
import { useTranslation } from 'react-i18next';
// ✅ 추가: cn 유틸 import
import { cn } from '../utils/cn';

interface LanguageSwitcherProps {
  flags?: { ko: string; de: string; en: string };
  className?: string;
}

export const LanguageSwitcher = ({ flags, className }: LanguageSwitcherProps = {}) => {
  const [currentLang, setCurrentLang] = useState<'ko' | 'de' | 'en'>('ko');
  const { i18n } = useTranslation();

  useEffect(() => {
    if (i18n.language !== currentLang) {
      setCurrentLang(i18n.language as 'ko' | 'de' | 'en');
    }
  }, [i18n.language]);

  const changeLanguage = (lang: 'ko' | 'de' | 'en') => {
    i18n.changeLanguage(lang);
    setCurrentLang(lang);
  };

  return (
    <div className={cn('flex items-center gap-2', className)}>
      {(['ko', 'de', 'en'] as const).map((lang) => (
        <button
          key={lang}
          onClick={() => changeLanguage(lang)}
          className={cn(
            'rounded-lg border px-3 py-2 text-xs font-medium transition-colors',
            currentLang === lang
              ? 'text-white bg-white/10 border-white/20'
              : 'text-slate-400 hover:text-slate-200 hover:bg-white/5 border-transparent'
          )}
          title={lang === 'ko' ? 'Korean' : lang === 'de' ? 'German' : 'English'}
        >
          {lang.toUpperCase()}
        </button>
      ))}
    </div>
  );
};
```

---

## 🟠 수정 5: `GitHubDeploy.test.tsx` — jsdom `innerText` 문제

**문제**: jsdom에서 `btn.innerText`가 `undefined`를 반환합니다.

```tsx
// ❌ 기존 (실패)
const deployBtn = Array.from(allButtons).find((btn) => {
  const classes = btn.className || '';
  return classes.includes('rounded-xl') && btn.innerText.includes('GitHub');
});

// ✅ 수정: textContent 사용 + disabled 상태 기반 탐색
const deployBtn = Array.from(allButtons).find((btn) => {
  const classes = btn.className || '';
  const text = btn.textContent || '';
  return classes.includes('rounded-xl') && !btn.disabled && text.includes('GitHub');
});
```

전체 테스트 파일의 3곳(`line 74`, `line 113`, `line 161`) 모두 동일 패턴으로 수정:

```tsx
// 모든 곳에서 btn.innerText → btn.textContent 로 변경
return classes.includes('rounded-xl') && !btn.disabled;
```

---

## 🟠 수정 6: `interpreter.ts` — struct/union 파싱 버그

**문제 1**: `parseStructDef()`에서 `this.eat(';')`가 `}` 뒤의 `;`를消費하기 전에 태그 이름을 읽으려 함.

**문제 2**: `typedefNames`가 `Set`인데 `.set()` 호출 시도.

### `interpreter.ts` 내 `parseStructDef` 수정:

```typescript
parseStructDef() {
  this.next(); /* consume 'struct' */
  const structName = this.expectId(); /* struct tag name */
  this.eat('{');
  const members: { name: string; type: string }[] = [];
  while (!this.isOp('}')) {
    const mtype = this.parseType();
    const mname = this.expectId();
    members.push({ name: mname, type: mtype });
    this.eat(';'); /* 각 멤버 뒤의 세미콜론 */
  }
  this.eat('}');
  this.eat(';'); /* struct 정의 끝 세미콜론 */
  
  // ✅ 수정: Set에는 .add() 사용
  this.typedefNames.add(structName);
  this.structDefs.set(structName, { members, size: members.length * 4 });
}
```

### `parseProgram()` 내 struct 변수 선언 부분 수정:

```typescript
if (this.cur().t === 'id' && TYPE_WORDS.has(this.cur().v)) {
  const ty = this.parseType();
  if (ty === 'struct') {
    // ✅ struct 타입 변수 선언: struct <tag> <varname>;
    // parseType이 'struct' 키워드만 소비하고 tag는 남김
    const tagName = this.expectId();
    if (!this.structDefs.has(tagName)) {
      throw new CError(this.cur().line, `알 수 없는 struct '${tagName}'`);
    }
    const varName = this.expectId();
    const items = this.parseDeclRest(varName, false);
    this.eat(';');
    return { k: 'decl', items, line, structTag: tagName } as Stmt;
  }
  // ... 나머지 처리
}
```

---

## 🟡 수정 7: `storage.ts` — 오타 수정

```typescript
// ❌ 기존
export const AUTOEMBER_SAVED_PROJECTS = 'autoembed-saved-projects';

// ✅ 수정
export const AUTOEMBED_SAVED_PROJECTS = 'autoembed-saved-projects';
export const AUTOEMBED_PROJECT_PROGRESS = 'autoembed-project-progress';
export const AUTOEMBED_ACTIVITY = 'autoembed-activity';
export const AUTOEMBED_EVENTS = 'autoembed-events';
export const AUTOEMBED_DIAGNOSIS = 'autoembed-diagnosis';
export const AUTOEMBED_INTERVIEW_STATS = 'autoembed-interview-stats';
export const AUTOEMBED_GERMAN_STATS = 'autoembed-german-stats';
export const AUTOEMBED_CHECKLIST = 'autoembed-checklist';
export const AUTOEMBED_DEADLINE = 'autoembed-deadline';
export const AUTOEMBED_LAB_SOLVED = 'autoembed-lab-solved';
export const AUTOEMBED_REPO_SOLVED = 'autoembed-repo-solved';
export const AUTOEMBED_CUSTOM_LABS = 'autoembed-custom-labs';
export const AUTOEMBED_APPLY = 'autoembed-apply';
export const AUTOEMBED_BACKUP = 'autoembed-backup';
```

---

## 📁 최종 파일 구조 변경 사항

```
변경:
  public/locales/ko/translation.json  →  src/locales/ko/translation.json
  public/locales/de/translation.json  →  src/locales/de/translation.json
  public/locales/en/translation.json  →  src/locales/en/translation.json

수정:
  src/providers/I18nProvider.tsx     (import 경로 + initReactI18next)
  src/App.tsx                        (import 추가 + ErrorBoundary 정의)
  src/components/LanguageSwitcher.tsx (cn import 추가)
  src/components/GitHubDeploy.test.tsx (innerText → textContent)
  src/offline/interpreter.ts         (struct/union 파싱 수정)
  src/utils/storage.ts              (오타 수정)
  package.json                       (의존성 추가)
```

---

## ✅ 수정 후 빌드 확인

```bash
npm install
npm run build    # ✅ 성공 예상
npm test         # ✅ 123 tests passing 예상
```