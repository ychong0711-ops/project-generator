--- a/.github/workflows/ci.yml
+++ b/.github/workflows/ci.yml
@@ -2,9 +2,8 @@
 
 on:
   push:
-    branches: [ "main" ]
+    branches: [ "main", "arena/**" ]
   pull_request:
-    branches: [ "main" ]
 
 jobs:
   build-and-test:
@@ -23,6 +22,9 @@
     - name: Install dependencies
       run: npm ci
 
+    - name: Type check
+      run: npm run typecheck
+
     - name: Run tests
       run: npm test
 
